//fill movies with Name and rating, and according to rating value, tell if it's a good or bad movie
let movies = [];

while (true) {
  let name = prompt("Movie's name: ");
  if (name == "q") {
    break;
  }
}
